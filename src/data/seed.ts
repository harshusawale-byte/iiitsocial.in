// ============================================
// Empty defaults — no seed data
// Real users only. The platform populates naturally.
// ============================================

import { User, Post, Discussion, Blog, Poll, Community, Club, Event, Message, Conversation, Notification, Video, TrendingTopic } from '../types';

export const emptyUsers: User[] = [];
export const emptyPosts: Post[] = [];
export const emptyDiscussions: Discussion[] = [];
export const emptyBlogs: Blog[] = [];
export const emptyPolls: Poll[] = [];
export const emptyCommunities: Community[] = [];
export const emptyClubs: Club[] = [];
export const emptyEvents: Event[] = [];
export const emptyMessages: Message[] = [];
export const emptyConversations: Conversation[] = [];
export const emptyNotifications: Notification[] = [];
export const emptyVideos: Video[] = [];
export const emptyTrending: TrendingTopic[] = [];
